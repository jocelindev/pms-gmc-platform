services:
  - type: web
    name: pms-gmc-platform
    runtime: python
    plan: free
    buildCommand: python -m py_compile server.py start.py database/init_database.py
    startCommand: python start.py --host 0.0.0.0
    healthCheckPath: /api/health
